# npm uses .npmignore instead of .gitignore when it exists.
# We need .next/ in the published package (pre-built for npx usage),
# so we do NOT ignore it here even though .gitignore does.

node_modules
.git
coverage
playwright-report
test-results
e2e
.env*
*.tsbuildinfo
.DS_Store
